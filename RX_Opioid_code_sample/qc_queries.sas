proc sql; create table drug_count1 as select "RefOxyContin" as comparator, sum(case when OP1F_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count2 as select "AnyOxyContin" as comparator, sum(case when OP1A_use=1 or OP1F_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count3 as select "Morphine" as comparator, sum(case when OP8A_use=1 or OP8B_use=1 or OP8C_use=1 or OP8D_use=1 or OP8E_use=1 or OP8F_use=1 or OP8G_use=1 or OP8H_use=1 or OP8I_use=1 or OP8J_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count4 as select "MSContin" as comparator, sum(case when OP8A_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count5 as select "MorphineIR" as comparator, sum(case when OP8B_use=1 or OP8G_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count6 as select "MorphER" as comparator, sum(case when OP8A_use=1 or OP8C_use=1 or OP8D_use=1 or OP8E_use=1 or OP8F_use=1 or OP8H_use=1 or OP8I_use=1 or OP8J_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count7 as select "HydrocoIR" as comparator, sum(case when OP3A_use=1 or OP3B_use=1 or OP3C_use=1 or OP3D_use=1 or OP3E_use=1 or OP3F_use=1 or OP3H_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count8 as select "OtherSchII" as comparator, sum(case when OP3A_use=1 or OP3B_use=1 or OP3C_use=1 or OP3D_use=1 or OP3E_use=1 or OP3F_use=1 or OP3H_use=1 or OP18A_use=1 or OP3I_use=1 or OP9A_use=1 or OP9B_use=1 or OP9D_use=1 or OP9F_use=1 or OP9G_use=1 or OP9H_use=1 or OP9I_use=1 or OP9J_use=1 or OP7A_use=1 or OP7C_use=1 or OP7E_use=1 or OP7F_use=1 or OP7H_use=1 or OP7I_use=1 or OP7B_use=1 or OP8A_use=1 or OP8B_use=1 or OP8C_use=1 or OP8D_use=1 or OP8E_use=1 or OP8F_use=1 or OP8G_use=1 or OP8H_use=1 or OP8I_use=1 or OP8J_use=1 or OP2A_use=1 or OP2B_use=1 or OP2C_use=1 or OP2D_use=1 or OP2E_use=1 or OP2F_use=1 or OP2H_use=1 or OP16A_use=1 or OP16B_use=1 or OP16C_use=1 or OP16D_use=1 or OP16E_use=1 or OP16F_use=1 or OP16G_use=1 or OP16H_use=1 or OP16I_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count9 as select "OxymorphER" as comparator, sum(case when OP9A_use=1 or OP9D_use=1 or OP9H_use=1 or OP9I_use=1 or OP9J_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count10 as select "OxycIRSE" as comparator, sum(case when OP2E_use=1 or OP16A_use=1 or OP16C_use=1 or OP16E_use=1 or OP16F_use=1 or OP16G_use=1 or OP16H_use=1 or OP16I_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count11 as select "OxycIRComb" as comparator, sum(case when OP2A_use=1 or OP2B_use=1 or OP2C_use=1 or OP2D_use=1 or OP2F_use=1 or OP2H_use=1 or OP16B_use=1 or OP16D_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count12 as select "OxycIR" as comparator, sum(case when OP2A_use=1 or OP2B_use=1 or OP2C_use=1 or OP2D_use=1 or OP2E_use=1 or OP2F_use=1 or OP2H_use=1 or OP16A_use=1 or OP16B_use=1 or OP16C_use=1 or OP16D_use=1 or OP16E_use=1 or OP16F_use=1 or OP16G_use=1 or OP16H_use=1 or OP16I_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
proc sql; create table drug_count13 as select "Methadone" as comparator, sum(case when OP4A_use=1 or OP4B_use=1 or OP4D_use=1 or OP4E_use=1 or OP4F_use=1 then 1 else 0 end) as abuse_count from outf.basefile_drugvars;quit;
